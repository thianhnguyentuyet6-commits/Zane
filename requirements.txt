fastapi==0.115.0
uvicorn==0.32.0
psutil==6.1.0
pillow==11.0.0
pydantic==2.9.2
python-multipart==0.0.12
aiofiles==24.1.0
httpx==0.27.2

# A夯实 - 基础加固
filelock==3.15.0
slowapi==0.1.9
APScheduler==3.10.4
python-jose[cryptography]==3.3.0

# Windows深度控制
# wmi==1.5.1  # Windows only, 安装脚本处理
# pywin32==308  # Windows only

# C自主 - 向量+OCR 轻量优先，重型可选
# 轻量已安装 - 补全
sqlite-vec==0.1.6
rapidocr_onnxruntime==1.3.12  # 轻量OCR 50MB，替代PaddleOCR 500MB，已安装
# 重型可选 - 用户选择时安装
# sentence-transformers==3.3.1  # 80MB语义，安装后自动启用
# torch==2.5.0  # 由sentence-transformers依赖
# paddlepaddle==3.0.0  # 重型
# paddleocr==2.9.1  # 重型 500MB

# 自我进化
datasets==3.0.0
# 可选训练：unsloth, torch, transformers, trl, peft
